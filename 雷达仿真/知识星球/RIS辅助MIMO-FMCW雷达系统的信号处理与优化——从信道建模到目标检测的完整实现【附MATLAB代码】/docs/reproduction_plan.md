# Reproduction Plan

## Stage 4 后的路线调整

Stage 4 已完成单目标 FMCW RD 检测链路。后续路线建议按以下顺序推进：

1. 保持 `fixed_grid_zf_snr` 为主线 RIS 相位优化器，先做单目标 RD 峰值的多随机信道统计。
2. 将 `generate_fmcw_echo.m` 的单目标输入自然扩展为多目标向量输入，目前函数已经支持 `range_m`、`velocity_mps`、`alpha` 的向量形式。
3. 在多目标 RD 图前，先明确幅度归一化策略，避免不同目标峰值过强或过弱导致图像解释混乱。
4. 再进入图5：多目标距离-时间或距离-慢时间幅度谱。
5. 最后进入图6：不同 RIS 单元数量下的 RD 图对比。

暂时不建议回到 ADMM/CD。当前更重要的是让 FMCW 回波、RD 轴、峰值检测和 ZF-SNR 优化增益之间的链路保持一致。

## Stage 3.4 后的路线调整

当前主线不再把论文 ADMM 闭式公式作为短期硬约束，而是先采用能稳定改善当前工程目标的 ZF-SNR-driven RIS phase optimizer。论文 ADMM、路径增益最大化和单位模约束仍作为理论参考，但后续图3/图4的主算法暂定从以下两个候选中选择：

1. `coarse_to_fine_zf_snr`
   - 优点：目标最直接，解释最清楚，优化的就是 ZF-SNR。
   - 缺点：相对 fixed grid 运行时间约翻倍。

2. `coarse_to_fine_zf_snr_with_condition_penalty`
   - 优点：Stage 3.4 最新 30 trial 中平均 SNR、平均条件数和平均 ZF raw power 略优。
   - 缺点：含经验参数 `alpha = 0.05`，后续需要确认不同 `Nr`、不同发射功率下是否稳定。

进入图3/图4前需要完成：

- 用多个 `Nr` 小规模扫描确认主算法不会因 RIS 单元数变化导致条件数或 ZF raw power 失控。
- 用多个发射功率小规模扫描确认主算法输出的 SNR 随功率单调增加。
- 明确 Monte Carlo 次数、随机种子管理、失败样本记录方式。
- 继续保留 `random_single` 和 `random_best_of_numStarts` 作为公平基线，防止把多初值收益误判为优化算法收益。

## 总体目标

逐步复现论文中的 RIS 辅助 MIMO-FMCW 雷达非视距目标参数估计流程，包括系统模型、ZF 预编码、RIS 相移优化、SNR 对比和距离-多普勒处理。

## 阶段 0：工程初始化

已完成：

- 创建 MATLAB 工程目录结构。
- 创建主脚本、配置文件和函数占位文件。
- 创建项目管理文档。
- 初步阅读论文，提取系统模型、估计模型、优化模型、算法流程和仿真参数。

## 阶段 1：参数和基础工具

目标：

- 完善 `config/paper_params.m`。
- 统一单位换算，例如 GHz、MHz、us、dBm、dB。
- 固定随机种子和输出路径。
- 明确 MATLAB 版本和必要工具箱。

验收标准：

- 所有主脚本能从同一个参数入口读取默认参数。
- 参数表与论文表1逐项对应。
- 未明确参数写入 `reproduction_assumptions.md`。

## 阶段 2：信道和 SNR 模块

目标：

- 实现 `generate_channels.m`。
- 实现 `compute_snr.m`。
- 建立等效信道 `H_eff = H_sr^H Phi H_rd Phi^H H_sr` 或代码中维度一致的等价形式。
- 检查矩阵维度、Hermitian 转置和 Frobenius 范数。

验收标准：

- 小尺寸随机矩阵测试通过。
- SNR 随发射功率上升，随噪声功率上升下降。
- 维度检查写入 debug 或测试记录。

## 阶段 3：ZF 预编码

目标：

- 实现 `design_precoder_zf.m`。
- 根据论文公式使用 Moore-Penrose 伪逆设计预编码矩阵。
- 加入功率归一化，使 `||B||_F^2 <= P`。

验收标准：

- 在条件良好的等效信道下，`H_eff * B` 接近单位阵或目标维度下的等价单位映射。
- 功率约束满足。

## 阶段 4：RIS 相移优化

目标：

- 实现 `optimize_ris_admm.m`。
- 实现 `optimize_ris_cd.m`。
- 记录目标函数、收敛准则、惩罚参数选择和初始化方式。

验收标准：

- ADMM 输出满足恒模约束。
- 目标值迭代曲线有收敛趋势。
- CD 作为基线可复现实验趋势。

## 阶段 5：复现图3和图4

目标：

- 图3：ADMM 与 CD 的 SNR 随 `N_r` 变化曲线。
- 图4：不同 `N_r` 下 ADMM 与 CD 的 SNR 随发射功率变化曲线。

验收标准：

- 曲线趋势与论文文字描述一致。
- 保存图、数据和运行日志。
- 每次实验更新 `experiment_log.md`。

## 阶段 6：FMCW 回波和距离-多普勒处理

目标：

- 实现 `generate_fmcw_echo.m`。
- 实现 `range_doppler_fft.m`。
- 完成多目标距离-时间谱和距离-多普勒谱。

验收标准：

- 单目标 sanity check 的距离和速度峰值位置正确。
- 多目标设置能区分论文中的四个目标。

## 阶段 7：复现图5和图6

目标：

- 图5：多目标距离-时间三维幅度谱。
- 图6：`N_r = 4, 16, 64` 下 ADMM 和 CD 的距离-多普勒图。

验收标准：

- 四个目标峰值位置可解释。
- 背景噪声和 RIS 单元数量变化趋势与论文描述一致。
- 保存所有图表和中间数据。
