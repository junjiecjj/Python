




clc;
clear all;
close all;
addpath('./functions');
rng(42);

rng(1);
N = 4;
K = 3;
L = 5;
PT = 1;

H = (randn(K, N) + 1j * randn(K, N)) / sqrt(2);
data = randi([0, 3], K, L);
S = pskmod(data, 4, pi / 4, 'gray');
tmp = randn(N, N) + 1j * randn(N, N);
R = A * A';
Rd = (R + R') / 2;

X1 = strict_waveform(H, S, Rd, L)




